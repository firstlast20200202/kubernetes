1. Create a new pod called nginx with the image nginx:alpine. The Pod should make use of the PVC local-pvc and mount the volume at the path /var/www/html.
The PVC local-pvc should be in a bound state.
A:
$ vi pod.yaml
apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:                        //
    name: myfrontend              //these two lines are for local-pvc binding and nginx pod running
spec:
  containers:
    - name: myfrontend
      image: nginx:alpine
      volumeMounts:
      - mountPath: "/var/www/html"
        name: mypd
  volumes:
    - name: mypd
      persistentVolumeClaim:
        claimName: local-pvc


connections of storage, volume, persistent volume, persistent volume claim, storage class:
volume是pod用的存储
persistent volume是cluster中的真实存储资源
PVC是用户申请存储的“请求单”
StorageClass定义如何创建PV

两种模式：
静态绑定：admins create PV, users create PVC, system binds them (then add pvc into pod file, that's how it's mounted)      //甚至用外部存储时比如AWS EBS还要先provision storage在上面
动态绑定：users create PVC, StorageClass auto creates PV (or auto-provision storage on AWS EBS), PVC binds PV              //StorageClass决定PV如何创建

备考：
Pod 用 Volume  
Volume 连 PVC  
PVC 找 PV  
PV 由 StorageClass 创建

关于provisioner:
StorageClass + provisioner = 自动创建 PV（动态存储）
provisioner 是“创建存储”的程序/插件
没有provisoner时：管理员手动创建PV -> 用户创建PVC -> 系统匹配已有 PV (PV事先存在，不会自动创建)
有provisoner时：用户创建 PVC -> StorageClass（带 provisioner）-> provisioner被触发-> 自动创建PV -> 绑定 PVC (PV 是在 PVC 创建时“动态生成”的)
StorageClass 的作用：
    provisioner: kubernetes.io/aws-ebs
    使用AWS EBS来创建磁盘
    当PVC来了 → 自动创建一个云盘
核心区别：PV是按需创建的(on demand)，不是提前准备好的
summary：provisioner之所以代表“动态存储”，是因为它能在PVC创建时自动创建PV，而不是依赖已有资源。
