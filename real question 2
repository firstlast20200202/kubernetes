$ k -n sound-repeater get ingressclasses
nginx

$ vi ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: echo
  namespace: sound-repeater
  annotations: 
    nginx.ingress.kubernetes.io/rewrite-target: /                //there's a space before /
spec:
  ingressClassName: nginx
  rules:
  - host: "example.org"
    http:
      paths:
      - path: /echo
        pathType: Prefix
        backend:
          service:
            name: echoserver-service 
            port:
              number: 8080

$ k -n sound-repeater apply -f ingress.yaml

//verify
$ curl http://example.org/echo      //就是curl命令加上题目给的url测试


annotation 是由控制器实现的，比如：
NGINX Ingress Controller
Traefik
HAProxy

考试时如何搜annotation：
Ingress 的 annotation 不在 Kubernetes 官方 API 文档里，而在具体 Ingress Controller 的文档中
搜nginx ingress 
点Before you migrate
搜rewrite
