candidate@master01:~$ ssh cka000059

candidate@master01:~$ sudo -i                //change user from candidate to root
root@master01:~# 

sh etcd-set.sh

kubectl get nodes

kubectl get pods -A                        //结果显示kube-system namespace的kube-scheduler-master01的status异常：这个在第二部分修复

k -n kube-system get pods    

kubectl describe pod -n kube-system kube-scheduler-master01          //检查message

vi /etc/kubernetes/manifests/kube-apiserver.yaml                                                    
//edit this field to:
//--etcd-servers=https://127.0.0.1:2379                //取下面两个url的任何一个放到这里

//where to get ths value:
//vi /etc/kubernetes/manifests/etcd.yaml
//--listen-client-urls=https://127.0.0.1:2379,https://172.30.30.11:2379

//重启kubelet
systemctl daemon-reload
systemctl restart kubelet


//第二部分
kubectl get nodes
kubectl -n kube-system get pod

lscpu | grep CPU
vim /etc/kubernetes/manifests/kube-scheduler.yaml
//update requests.cpu: 200m

systemctl daemon-reload                                    //加载所有配置文件
systemctl restart kubelet                                  //重启服务使加载生效

//verify
kubectl get nodes
kubectl -n kube-system get pod

//remember:             
root@master01:~# exit          //quit to base node
candidate@master01:~$ exit     //quit cka000059

