wget https://raw.githubusercontent.com/projectcalico/calico/v3.27.0/manifests/tigera-operator.yaml

k create -f tigera-operator.yaml      //dont user apply here which would conflict with CRD

kubectl cluster-info dump | grep -i cluster-cidr                      //copy the ip address (dump here means -o)
                            "--cluster-cidr=10.244.0.0/16",

wget https://raw.githubusercontent.com/projectcalico/calico/v3.27.0/manifests/custom-resources.yaml          //use the same url as above just replace file name to custom-resources.yaml

vi custom-resources.yaml          //replace the cidr field with the ip address searched above

kubectl create -f custom-resources.yaml

//verify
kubectl -n calico-system get pod


CNI是让pod拥有IP并能互相通信的一套规则+插件机制
当一个pod创建时，CNI分配一个IP地址，连接到网络，和其它pod通信
常见CNI插件：
Calico（支持 NetworkPolicy）
Flannel（简单，但不支持 NetworkPolicy）
