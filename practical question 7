helm repo add argo https://argoproj.github.io/argo-helm/

helm repo update

helm search repo argo | grep argo-cd                        //get version number

helm template argocd argo/argo-cd --version 5.5.22 --namespace argocd --set crds.install=false > ~/argo-helm.yaml

helm install argocd argo/argo-cd --version 5.5.22 --namespace argocd --set crds.install=false

//verify
kubectl -n argocd get pods





拆解：
helm template argocd argo/argo-cd             //把argocd helm chart渲染成kubernetes yaml，但不安装在集群中
helm template          //helm的纯渲染模式，输出的是kubernetes yaml，不会：创建资源、改变集群状态、记录release；这个命令相当于“预览安装结果”


argocd的作用：
以前kubectl apply -f app.yaml 
现在用argocd的作用：
Git 仓库（YAML）
        ↓
Argo CD 自动检测变化
        ↓
自动部署到 Kubernetes

你只需要：
修改 Git 和 push，Argo CD 自动帮你部署

Argo CD 的 Helm Chart 就是用 Helm 打包好的 Argo CD 安装包
